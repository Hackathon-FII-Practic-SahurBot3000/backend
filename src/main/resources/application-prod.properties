spring.application.name=app

server.port=8080

# Database Configuration - Use environment variables for security
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://octavianregatun.com:5432/hackathon-fiipractic}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:9234}

# Production JPA settings - more conservative than development
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:validate}
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.use_sql_comments=false

# Connection pool optimization for production
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=600000

# API Documentation
springdoc.swagger-ui.path=/swagger-ui
springdoc.api-docs.enabled=${API_DOCS_ENABLED:true}
springdoc.swagger-ui.enabled=${SWAGGER_UI_ENABLED:true}

# JWT Configuration - Use environment variables
jwt.secret=${JWT_SECRET:7yqwYBqYu8b1bedddyeszUCwIaB962Q/5W2Bwv3RQ7I=}
jwt.expiration=${JWT_EXPIRATION:86400000}

# OAuth2 Configuration - Use environment variables
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:476589245232-je77eht00s9a8ild559f4omu6v1j3ik6.apps.googleusercontent.com}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET: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}
spring.security.oauth2.client.registration.google.scope=email,profile

# Application settings
spring.main.allow-circular-references=true

# CORS and Frontend URL
frontend.url=${FRONTEND_URL:https://hackathon-fiipractic.octavianregatun.com}

# Logging configuration for production
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN
logging.level.com.zaxxer.hikari=WARN
logging.level.root=INFO

# Actuator endpoints for monitoring
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized
management.metrics.export.simple.enabled=true